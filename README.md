# DAT-Merge-Tool - Инструмент для сравнения и слияния файлов

Веб-инструмент для сравнения двух файлов (DAT, JSON, ItemName и других форматов), поиска измененных полей и быстрой замены значений на нужный вариант. Позволяет эффективно работать с большими объемами данных благодаря фильтрам, массовым операциям и интуитивному интерфейсу.

## Структура проекта

```
DiffChange/
├── pom.xml
├── src/
│   ├── main/
│   │   ├── java/
│   │   │   └── org/
│   │   │       └── example/
│   │   │           ├── Main.java                    # Точка входа
│   │   │           ├── comparator/
│   │   │           │   ├── DataComparator.java      # Контракт компараторов
│   │   │           │   ├── FileComparator.java      # Обход директорий
│   │   │           │   ├── JsonComparator.java      # Сравнение JSON
│   │   │           │   └── ItemNameComparator.java  # Сравнение блоков item_name_*
│   │   │           ├── dto/
│   │   │           │   └── DiffResult.java          # DTO для результатов
│   │   │           └── writer/
│   │   │               └── DiffWriter.java           # Запись результатов
│   │   └── resources/
│   └── test/
└── diff/                                             # Директория с тестовыми файлами-примерами для сравнения
    ├── old/                                          # Тестовые файлы-примеры старых версий (OLD)
    │   ├── ItemName_ClassicAden-eu.txt              # Тестовый ItemName файл
    │   ├── NpcString-ru.dat.txt                     # Тестовый DAT файл
    │   └── NpcString_Classic-ru.dat.txt            # Тестовый DAT файл
    ├── new/                                          # Тестовые файлы-примеры новых версий (NEW)
    │   └── ItemName_ClassicAden-eu.txt              # Тестовый ItemName файл
    └── output/                                       # Результаты сравнения (JSON)
        └── ItemName_ClassicAden-eu.txt.diff.json    # Пример результата сравнения
```

## Основные возможности

### Сравнение файлов
- 📊 **Сравнение двух файлов** (OLD и NEW) с визуальным отображением различий
- 🔍 **Поиск измененных полей** - автоматическое выявление всех изменений между файлами
- 📋 **Отображение в трех колонках**: OLD (старое значение), NEW (новое значение), MERGED (результат слияния)

### Фильтрация и поиск
- 🔎 **Поиск по ID и полям** - быстрый поиск нужных записей
- ✂️ **Фильтр "Показать только изменения"** - скрывает идентичные записи
- 🎯 **Фильтр "Показать только измененные поля"** - показывает только поля с различиями, скрывает записи без изменений

### Работа с данными
- ⚡ **Быстрая замена значений** - кнопки "Принять OLD" и "Принять NEW" для каждого поля
- 🔄 **Массовые замены** - группировка похожих изменений и применение ко всем записям одновременно
- ✏️ **Редактирование MERGED** - возможность вручную редактировать итоговое значение
- 💾 **Экспорт результата** - сохранение объединенного файла

### Удобство использования
- 📁 **История последних файлов** - сохранение недавно загруженных файлов для быстрого доступа
- 📊 **Статистика изменений** - отображение количества добавленных, удаленных и измененных записей
- 🎨 **Удобный интерфейс** - темная тема, виртуальная прокрутка для больших файлов
- 🚀 **Высокая производительность** - оптимизированная работа с файлами любого размера

### Поддерживаемые форматы
- ✅ DAT-файлы (расшифрованные)
- ✅ JSON-файлы
- ✅ ItemName файлы (`item_name_begin` / `item_name_end`)
- ✅ Конфигурационные файлы (INI / `section` + `key=value`)

## Требования

- Java 21+
- Maven 3.6+

## Установка и запуск

### Сборка проекта

```bash
mvn clean package
```

### Запуск приложения

```bash
mvn spring-boot:run
```

Или после сборки:

```bash
java -jar target/diffchange-1.0-SNAPSHOT.jar
```

Приложение будет доступно по адресу: `http://localhost:8080`

### Использование

1. Откройте веб-интерфейс в браузере
2. Загрузите два файла: OLD.txt (старая версия) и NEW.txt (новая версия)
3. Просмотрите различия в удобном интерфейсе
4. Используйте фильтры для поиска нужных изменений
5. Примените замены (по одной или массово)
6. Экспортируйте результат

## Форматы входных данных

### JSON

Полностью поддерживаются объекты и массивы. Для массивов элементы сопоставляются по полю `id`. Изменения значений фиксируются в `changed`, добавление/удаление полей — в `structureChanges`.

### ItemName (`*.txt`)
- ### Конфигурационные (INI) файлы

Файлы с секциями `[Section]` и строками `ключ=значение`. При сравнении каждая пара `section.key` считается отдельной записью. Результат экспорта повторяет исходный формат: секции и их порядок сохраняются.


Файлы вида `ItemName_ClassicAden-eu.txt`, где каждый объект обрамлен маркерами:

```
item_name_begin
    id=94174    name=[Some name]    additionalname=[Sealed]    ...
    description=[Текст описания]
item_name_end
```

- Внутри блока поле задается как `ключ=значение` или `ключ=[значение]`.
- Поля разделены табуляцией (`\t`), но могут располагаться и в новых строках.
- Объекты сравниваются по `id`.
- Изменения значений попадают в `changed`, появление/удаление полей — в `structureChanges`.

## Интерфейс

Веб-интерфейс предоставляет удобный способ работы с различиями:

- **Три колонки данных**: OLD (красный фон), NEW (синий фон), MERGED (зеленый фон)
- **Заголовки колонок**: "ПОЛЕ", "OLD", "NEW", "MERGED" для удобной навигации
- **Кнопки действий**: 
  - "Принять OLD" - копирует значение из OLD в MERGED
  - "Принять NEW" - копирует значение из NEW в MERGED
  - "Удалить" - удаляет запись из результата
- **Массовые действия**: группировка похожих изменений для одновременного применения
- **Экспорт**: сохранение объединенного файла с примененными изменениями

## Формат данных

Приложение работает с файлами, где записи идентифицируются по полю `id`. Каждая запись содержит набор полей (ключ-значение). При сравнении:

- **Измененные поля** - поля с разными значениями в OLD и NEW
- **Добавленные записи** - записи, присутствующие только в NEW
- **Удаленные записи** - записи, присутствующие только в OLD
- **Неизмененные записи** - записи без различий (скрываются фильтром)

## Примеры использования

### Тестовые файлы

В директории `diff/` находятся тестовые файлы-примеры для тестирования:
- `diff/old/` - содержит тестовые файлы-примеры старых версий (OLD)
- `diff/new/` - содержит тестовые файлы-примеры новых версий (NEW)
- `diff/output/` - содержит результаты сравнения в формате JSON

Вы можете использовать эти тестовые файлы для знакомства с инструментом или добавить свои файлы для сравнения.

### Пример 1: Сравнение ItemName файлов

1. Загрузите файлы из `diff/old/ItemName_ClassicAden-eu.txt` (OLD) и `diff/new/ItemName_ClassicAden-eu.txt` (NEW)
2. Включите фильтр "Показать только измененные поля" для фокусировки на различиях
3. Найдите нужные записи через поиск по ID или содержимому
4. Используйте кнопки "Принять OLD" или "Принять NEW" для каждого поля
5. Для массовых замен используйте кнопку "Массовые действия" - инструмент автоматически найдет похожие изменения
6. Экспортируйте результат после применения всех изменений

### Пример 2: Работа с большими файлами

1. Загрузите файлы с тысячами записей (например, из директории `diff/`)
2. Используйте поиск для быстрой навигации по ID или содержимому полей
3. Примените фильтры:
   - "Показать только изменения" - скрывает идентичные записи
   - "Показать только измененные поля" - показывает только поля с различиями
4. Используйте массовые операции для групповых замен похожих изменений
5. Просмотрите статистику изменений в панели "Статистика"
6. Сохраните результат через кнопку "Экспорт"

## Архитектура

Проект следует принципам SOLID:

- **Single Responsibility**: каждый класс отвечает за одну задачу
- **Open/Closed**: легко расширяется для поддержки других форматов (YAML, CSV)
- **Liskov Substitution**: интерфейсы позволяют заменять реализации
- **Interface Segregation**: четкое разделение ответственности
- **Dependency Inversion**: зависимости через конструкторы

### Расширяемость

Новые форматы подключаются через реализацию `DataComparator`:

1. Создайте класс, реализующий `DataComparator` (например, `YamlComparator`)
2. Опишите логику `supports`, `compare` и `compareWithMissing`
3. Добавьте компаратор в список в `Main`

## Обработка ошибок

- Поврежденный JSON: выводится предупреждение, файл пропускается
- Отсутствующий файл: отмечается как added/removed
- Ошибки доступа: логируются, не прерывают выполнение

## Логирование

Используется SLF4J с Simple Logger. Логи выводятся в консоль с уровнями:
- INFO: общая информация о процессе
- WARN: предупреждения (поврежденный JSON и т.д.)
- ERROR: критические ошибки
